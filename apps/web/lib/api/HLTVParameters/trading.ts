export const getTradingValue = async (
	steamId?: string,
	date: string = "all",
) => {
	const tradingParameters = getTradingParameters(steamId, date);
};

const getTradingParameters = async (
	steamId?: string,
	date: string = "all",
) => {};
