export const getUtilityValue = async (
	steamId?: string,
	date: string = "all",
) => {
	const utilityParameters = getUtilityParameters(steamId, date);
};

const getUtilityParameters = async (
	steamId?: string,
	date: string = "all",
) => {};
