export const getEntryingValue = async (
	steamId?: string,
	date: string = "all",
) => {
	const entryingParameters = getEntryingParameters(steamId, date);
};

const getEntryingParameters = async (
	steamId?: string,
	date: string = "all",
) => {};
